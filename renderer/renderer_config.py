class RendererConfig:
    def __init__(self, screen_config, app_config, x_pos=0, y_pos=0):
        self.screen_config = screen_config
        self.app_config = app_config
        self.x_pos = x_pos
        self.y_pos = y_pos
